
const letters = ['red', 'green', 'blue']
const index = letters.indexOf('green');
function checkColor(arr) {
    for (i = 0; i < arr.length; ++i) {
        console.log(arr[i]);
    }
}
console.log(letters);
checkColor(console.log(index));
